% eq-theta-eps.tex -- inline equation for the course page, rendered to SVG by the
% Makefile (latex -> dvisvgm --no-fonts), then placed in a lesson bullet as an
% <img class="eq"> sized in em from the SVG's own height.
\documentclass[12pt,border=1pt]{standalone}
\usepackage{amsmath,amssymb}
\usepackage{xcolor}
\definecolor{ink}{HTML}{2D3140}
\begin{document}
\color{ink}
$\Theta(1/\varepsilon)$
\end{document}
